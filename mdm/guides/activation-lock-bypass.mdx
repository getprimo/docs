---
title: "Activation Lock Bypass Codes"
description: "Learn how to unlock a supervised Mac device using an Activation Lock bypass code, including retrieval via Primo, entry instructions for different macOS versions, troubleshooting tips, and important legal considerations for responsible usage."
---

This article explains how to use an **Activation Lock bypass code** to unlock a supervised Mac device. It details the conditions required for retrieving the code via Primo, how to enter it on macOS, and what to do in case of failure.

## Understand Activation Lock

Activation Lock is an Apple security feature that prevents unauthorized use of iOS and macOS devices if they are lost or stolen. It is automatically enabled when a user signs in with an Apple ID and activates **Find My** on the device.

For more information, see [Apple documentation](https://support.apple.com/en-us/102648).

<Note>
After a reset, the device remains linked to the associated Apple ID and cannot be reused without it.
</Note>

## Understand the bypass code

The bypass code is a unique identifier that allows you to unlock a device **without using the original Apple ID or password** after a reset. This code is typically generated by the device owner or administrator.

If the device is supervised via an MDM like Primo, the bypass code can be stored automatically, enabling secure and controlled resets.

<Tip>
If the MDM cannot retrieve the bypass code, only the original user can unlock the device using their Apple ID credentials.
</Tip>

## Retrieve the bypass code in Primo

When available, the bypass code can be accessed directly from the Primo interface:

1. Go to **Devices > All Devices**
2. Open the relevant device record
3. Scroll down to the **Compliance** section
4. Check the **iCloud Lock** status:
   - ✅ *Enabled*: the bypass code is available
   - ❌ *Missing bypass code*: no code was backed up

## Use bypass code

You can then use it according to the different macOS versions:

### For macOS Catalina (10.15) and later versions

1. Make sure your Mac is connected to the Internet.
2. On the activation lock screen, click on the question mark icon next to the Apple ID field.
3. Select "Bypass activation lock".
4. Enter the activation lock bypass code provided.
5. Click "Unlock" to remove the activation lock.

### For macOS Mojave (10.14) and earlier versions

1. Connect your Mac to the Internet.
2. On the activation lock screen, enter the bypass code directly in the password field.
3. Click "Unlock" to remove the activation lock.

## Troubleshooting

If the bypass code does not work:

1. Make sure the code was entered correctly (case-sensitive).
2. Check the internet connection.
3. Restart the device and try again.
4. Contact your device administrator or Apple Support if the issue persists.

If no bypass code was stored or available:

- The device cannot be unlocked without the **Apple ID** used previously.
- Only the user associated with the device can remove iCloud Lock.
- If the Apple ID is no longer accessible, contact **Apple Support**.

## Important notes

- The bypass code is generally a single-use code for each device.
- It is case-sensitive. Enter it exactly as supplied.
- For company-owned devices, make sure you have authorization from your IT department.

## Legal and ethical considerations

Only use the activation lock bypass code on devices you own or for which you have explicit authorization to unlock. Unauthorized use may be illegal.

<Warning>
The activation lock bypass code is a powerful tool. Use it responsibly and only when you have the right to do so.
</Warning>
