---
title: "Password policy"
description: "Enforce password policies, including minimum length and complexity, with session password renewal managed by FleetDM, applicable on both macOS and Windows, while ensuring compliance with recent changes in macOS interface behavior."
---

## OS Support

| macOS                         | Windows | Linux | iOS / iPadOS | Android |
| ----------------------------- | ------- | ----- | ------------ | ------- |
| ✅ macOS 13+ (excl. 14.0–14.2) | ✅       |       | ✅            |         |

## On macOS

<Warning>
  **About the Screen Lock Policy**

  Due to a recent change in macOS, the screen lock value enforced by Primo is no longer displayed on the device and is no longer shown as grayed out in System Settings.

  This is a macOS-side behavior (UI only), not a change in MDM.

  Even though users may appear able to select a different screen lock timeout, the MDM-enforced value still applies and always takes priority.
</Warning>

- Renewal: The system does not require an immediate password reset when this feature is activated but is based on the date of the last modification. Password renewal will then be requested at regular intervals according to the requested configuration.
- Robustness: The system does not require an immediate reset but applies at the next password renewal. You can therefore combine renewal and robustness to ensure a quick update of all session passwords.
- Password Implicit requirements
  - Cannot contain repeated sequences of the same character (e.g. 'sss', 'fff...')
  - Cannot use consecutive double (or more) identical characters anywhere in the password
  - Certain keywords (like "F1rst") and easily guessable series are not allowed

_This feature is available on macOS 13 and later versions, except for macOS Sonoma 14.0 to 14.2 inclusive_.

## On Windows

- Renewal:
  - PIN: The system forces the PIN update at the next login if it does not comply.
  - Traditional passwords: The system does not require an immediate password reset when this feature is activated but is based on the date of the last modification. Password renewal will then be requested at regular intervals according to the requested configuration.
- Robustness: The system does not require an immediate reset but applies at the next password renewal. You can therefore combine renewal and robustness to ensure a quick update of all session passwords.

<Warning>
  Any changes to this password policy will require all Windows users to reset their password at the next sign-in. This behavior is enforced by Windows.

  [Learn more](https://learn.microsoft.com/en-us/windows/client-management/mdm/policy-csp-devicelock)
</Warning>

## On iOS / iPadOS

Password and screen lock policies apply to both PIN codes and traditional passwords on supervised iOS and iPadOS devices. Configure the iOS tab in the Password & Screen lock card within your MDM profile.

## Troubleshooting

If your new password is rejected:

- Double-check for any repeated (e.g. 'sss' or 'aaa') or consecutive letters
- Avoid using reserved words or easily guessable terms (like "F1rst")
- Make sure your password meets length and complexity rules
- If you still see an error, try a different variation or contact support